server:
  port: 8083
spring:
 datasource:
   driver: org.mariadb.jdbc.Driver
   url: jdbc:mariadb://localhost:3307/projectcode
   username: root
   password: rootroot
 jpa:
   show-sql: true
   hibernate:
     ddl-auto: update
   properties:
     hibernate:
       dialect: org.hibernate.dialect.MariaDB103Dialect